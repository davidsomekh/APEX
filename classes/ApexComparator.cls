/********************************************
*	EmilK
*
*
**********************************************/
public virtual class ApexComparator {
	
	/*
	*	EmilK
	*	0 - if objects are equal 
	*	1 -	if first is greater than the second 
	* 	-1 - if the second is graeted from the first
	*/
	public virtual Integer Compare(SObject first_obj, SObject second_obj){
		return 0;	
	}
}